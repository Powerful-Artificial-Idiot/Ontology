# Knowledge Instance Examples

`valid/` contains the minimum connected manufacturing scenario used by SHACL and competency-query tests. `invalid/` intentionally violates measurement and event constraints and must never pass validation.
