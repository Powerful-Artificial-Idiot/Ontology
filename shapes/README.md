# SHACL Shapes

SHACL validates ontology instances and business constraints. Frontend payload structure is validated separately by JSON Schema under `packages/knowledge-contracts/schemas`.

Run `make shapes-validate` to assert that valid fixtures conform and invalid fixtures are rejected.
